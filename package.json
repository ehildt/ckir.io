{
  "name": "@ckir.io/base",
  "repository": "git@github.com:ehildt/ckir.git",
  "private": true,
  "engines": {
    "node": ">=24",
    "pnpm": ">=10"
  },
  "scripts": {
    "prepare": "husky",
    "ncu:update": "npx npm-check-updates --interactive --format group --format group",
    "docpose": "docpose -c .docpose.yml",
    "changeset": "npx changeset",
    "test": "echo 'passing with no tests in ROOT!'"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.7",
    "dotenv-cli": "^10.0.0",
    "husky": "^9.1.7",
    "npm-check-updates": "^18.3.0"
  },
  "workspaces": [
    "apps/*",
    "libs/*"
  ]
}