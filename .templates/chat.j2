  chat:
    container_name: chat
    restart: on-failure
    build:
      context: ./apps/chat
      target: local
    volumes:
      - ./apps/chat:/app
      - ./node_modules:/node_modules  
    depends_on:
      - keydb
    env_file:
      - ./apps/chat/.env
    environment:
      - NODE_ENV=local
      - PRINT_CONFIG=true
      - ENABLE_SWAGGER=true
      - ENABLE_REST=true
    ports:
      - 3001:3001
      - 8081:8081
    networks:
      - re-sync-network
