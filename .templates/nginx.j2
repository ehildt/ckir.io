  nginx:
    image: nginx:latest
    container_name: nginx
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    ports:
      - "8080:80"  # Only Nginx is exposed to the host
    networks:
      - ckir.io-network